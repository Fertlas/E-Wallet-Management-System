name: ewallet_management_system
description: An absolute bare-bones web app.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.3.0

# Add regular dependencies here.
dependencies:
  web: ^0.4.0

dev_dependencies:
  build_runner: ^2.4.9
  build_test: ^2.2.2
  build_web_compilers: ^4.0.10
  lints: ^3.0.0
